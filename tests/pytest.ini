[pytest]
# Two tiers via markers. Bare `pytest` runs the offline siloed suite (e2e
# deselected by default); `pytest -m e2e` runs the dev/bin tier.
markers =
    e2e: end-to-end tests requiring aligner binaries (blastp/mmseqs2/ARC) + build data; run on arborist-dev
addopts = -m "not e2e"
